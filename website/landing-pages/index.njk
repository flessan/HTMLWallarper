---
layout: layout.njk
title: HTMLWallarper - Animated HTML Wallpapers for KDE Plasma
description: A collection of lightweight, animated HTML wallpapers for KDE Plasma. Zero CPU overhead, CSS-only animations, fully customizable.
permalink: landing-pages/index.html
---

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;500;600;700&family=Nunito:wght@400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">

{% raw %}
<style>
  /* ═══════════════════════════════════════════════════════════════
     CSS CUSTOM PROPERTIES - Design Tokens
     ═══════════════════════════════════════════════════════════════ */
  :root {
    /* Base Palette */
    --bg: #FEF8F0;
    --bg-alt: #FFF5E6;
    --bg-warm: #FFECD2;
    --bg-dot: #F5E1C8;
    
    /* Card System */
    --card: #FFFFFF;
    --card-alt: #FFFCF7;
    --card-border: #E8D5B8;
    --card-border-hover: #E8A85C;
    --card-shadow: rgba(61, 44, 30, 0.06);
    
    /* Text Hierarchy */
    --text: #2D1810;
    --text-soft: #4A3228;
    --text-muted: #7A5D4A;
    --text-light: #A08570;
    --text-faint: #C4A88A;
    
    /* Brand Colors - Warm Sunset Palette */
    --primary: #FF6B35;
    --primary-hover: #E85A24;
    --primary-light: rgba(255, 107, 53, 0.12);
    --primary-pale: rgba(255, 107, 53, 0.06);
    
    --secondary: #00B4A0;
    --secondary-hover: #009688;
    --secondary-light: rgba(0, 180, 160, 0.12);
    
    --tertiary: #7C4DFF;
    --tertiary-light: rgba(124, 77, 255, 0.12);
    
    --accent: #FFB300;
    --accent-light: rgba(255, 179, 0, 0.12);
    --accent-dark: #E65100;
    
    /* Extended Palette */
    --coral: #FF7043;
    --coral-light: rgba(255, 112, 67, 0.12);
    --mint: #4DB6AC;
    --mint-light: rgba(77, 182, 172, 0.12);
    --lavender: #B388FF;
    --lavender-light: rgba(179, 136, 255, 0.12);
    --rose: #F06292;
    --rose-light: rgba(240, 98, 146, 0.12);
    --sky: #42A5F5;
    --sky-light: rgba(66, 165, 245, 0.12);
    --lime: #9CCC65;
    --lime-light: rgba(156, 204, 101, 0.12);
    --amber: #FFCA28;
    --amber-light: rgba(255, 202, 40, 0.12);
    
    /* Semantic Colors */
    --success: #66BB6A;
    --success-light: rgba(102, 187, 106, 0.12);
    --warning: #FFA726;
    --warning-light: rgba(255, 167, 38, 0.12);
    --info: #29B6F6;
    --info-light: rgba(41, 182, 246, 0.12);
    
    /* Shadows */
    --shadow-xs: 0 2px 6px var(--card-shadow);
    --shadow-sm: 0 4px 12px var(--card-shadow);
    --shadow-md: 0 6px 20px var(--card-shadow);
    --shadow-lg: 0 12px 40px var(--card-shadow);
    --shadow-xl: 0 20px 60px var(--card-shadow);
    
    --shadow-card: 
      0 4px 0 0 var(--card-border),
      0 6px 20px var(--card-shadow);
    --shadow-card-hover:
      0 4px 0 0 var(--card-border-hover),
      0 10px 30px rgba(255, 107, 53, 0.12);
    --shadow-button:
      0 3px 0 0 var(--text),
      0 4px 12px var(--card-shadow);
    --shadow-button-hover:
      0 4px 0 0 var(--text),
      0 6px 20px rgba(255, 107, 53, 0.15);
    --shadow-glass: 0 8px 32px rgba(0, 0, 0, 0.04);
    
    /* Border Radius */
    --radius-xs: 0.375rem;
    --radius-sm: 0.625rem;
    --radius-md: 0.875rem;
    --radius-lg: 1.25rem;
    --radius-xl: 1.75rem;
    --radius-2xl: 2.25rem;
    --radius-full: 9999px;
    
    /* Spacing */
    --space-xs: 0.25rem;
    --space-sm: 0.5rem;
    --space-md: 1rem;
    --space-lg: 1.5rem;
    --space-xl: 2rem;
    --space-2xl: 3rem;
    --space-3xl: 4rem;
    --space-4xl: 6rem;
    
    /* Typography */
    --font-display: 'Fredoka', sans-serif;
    --font-body: 'Nunito', sans-serif;
    --font-mono: 'JetBrains Mono', monospace;
    
    /* Animation */
    --ease-out: cubic-bezier(0.22, 1, 0.36, 1);
    --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
    --bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);
    --spring: cubic-bezier(0.175, 0.885, 0.32, 1.275);
    
    --duration-fast: 150ms;
    --duration-normal: 250ms;
    --duration-slow: 400ms;
    --duration-slower: 600ms;
    
    /* Layout */
    --nav-height: 72px;
    --container-max: 1200px;
    --container-narrow: 800px;
  }

  /* Dark Theme */
  [data-theme="dark"] {
    --bg: #0F0D12;
    --bg-alt: #161318;
    --bg-warm: #1A161D;
    --bg-dot: #201D24;
    
    --card: #1C1920;
    --card-alt: #221E26;
    --card-border: #3A3542;
    --card-border-hover: #FF8A65;
    --card-shadow: rgba(0, 0, 0, 0.35);
    
    --text: #F5F0E8;
    --text-soft: #DED5C8;
    --text-muted: #9E918A;
    --text-light: #6E635C;
    --text-faint: #4A4340;
    
    --primary: #FF8A65;
    --primary-hover: #FFAB91;
    --primary-light: rgba(255, 138, 101, 0.14);
    --primary-pale: rgba(255, 138, 101, 0.06);
    
    --secondary: #4DD0C4;
    --secondary-light: rgba(77, 208, 196, 0.14);
    
    --tertiary: #B388FF;
    --tertiary-light: rgba(179, 136, 255, 0.14);
    
    --shadow-card: 
      0 4px 0 0 var(--card-border),
      0 6px 24px var(--card-shadow);
    --shadow-card-hover:
      0 4px 0 0 var(--card-border-hover),
      0 10px 35px rgba(255, 138, 101, 0.14);
  }

  /* ═══════════════════════════════════════════════════════════════
     BASE RESET & TYPOGRAPHY
     ═══════════════════════════════════════════════════════════════ */
  *, *::before, *::after {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  body {
    font-family: var(--font-body);
    font-size: 16px;
    line-height: 1.7;
    background: var(--bg);
    color: var(--text);
    overflow-x: hidden;
    transition: 
      background-color var(--duration-slow) var(--ease-in-out),
      color var(--duration-slow) var(--ease-in-out);
  }

  /* Dot Pattern Background */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    background-image: radial-gradient(var(--bg-dot) 1.2px, transparent 1.2px);
    background-size: 28px 28px;
    opacity: 0.5;
    transition: opacity var(--duration-slow);
  }

  [data-theme="dark"] body::before {
    opacity: 0.25;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: var(--font-display);
    font-weight: 600;
    line-height: 1.25;
    color: var(--text);
  }

  h1 { font-size: clamp(2.25rem, 6vw, 4rem); font-weight: 700; }
  h2 { font-size: clamp(1.75rem, 4vw, 2.75rem); }
  h3 { font-size: clamp(1.25rem, 2.5vw, 1.5rem); }
  h4 { font-size: 1.125rem; }

  p { color: var(--text-muted); }

  a {
    color: inherit;
    text-decoration: none;
    transition: color var(--duration-fast);
  }

  img {
    max-width: 100%;
    height: auto;
    display: block;
  }

  button {
    font-family: inherit;
    cursor: pointer;
    border: none;
    background: transparent;
  }

  code, pre {
    font-family: var(--font-mono);
  }

  ::selection {
    background: var(--primary);
    color: #fff;
  }

  :focus-visible {
    outline: 3px solid var(--primary);
    outline-offset: 3px;
    border-radius: var(--radius-xs);
  }

  /* Skip Link */
  .skip-link {
    position: fixed;
    top: -100px;
    left: var(--space-md);
    z-index: 99999;
    padding: var(--space-sm) var(--space-md);
    background: var(--primary);
    color: #fff;
    font-weight: 700;
    border-radius: var(--radius-sm);
    transition: top var(--duration-normal);
  }

  .skip-link:focus {
    top: var(--space-md);
  }

  /* ═══════════════════════════════════════════════════════════════
     UTILITY CLASSES
     ═══════════════════════════════════════════════════════════════ */
  .container {
    width: 100%;
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 var(--space-lg);
  }

  .container-narrow {
    max-width: var(--container-narrow);
  }

  .text-center { text-align: center; }
  .text-left { text-align: left; }
  .text-right { text-align: right; }

  .flex { display: flex; }
  .flex-col { flex-direction: column; }
  .items-center { align-items: center; }
  .justify-center { justify-content: center; }
  .justify-between { justify-content: space-between; }
  .gap-sm { gap: var(--space-sm); }
  .gap-md { gap: var(--space-md); }
  .gap-lg { gap: var(--space-lg); }

  .grid { display: grid; }
  .grid-cols-2 { grid-template-columns: repeat(2, 1fr); }
  .grid-cols-3 { grid-template-columns: repeat(3, 1fr); }
  .grid-cols-4 { grid-template-columns: repeat(4, 1fr); }

  @media (max-width: 768px) {
    .grid-cols-2, .grid-cols-3, .grid-cols-4 {
      grid-template-columns: 1fr;
    }
  }

  /* Section Spacing */
  .section {
    position: relative;
    padding: var(--space-4xl) 0;
    z-index: 1;
  }

  .section-sm {
    padding: var(--space-2xl) 0;
  }

  /* ═══════════════════════════════════════════════════════════════
     SCROLL PROGRESS INDICATOR
     ═══════════════════════════════════════════════════════════════ */
  #scrollProgress {
    position: fixed;
    top: 0;
    left: 0;
    height: 4px;
    z-index: 10001;
    background: linear-gradient(
      90deg,
      var(--primary),
      var(--secondary),
      var(--tertiary),
      var(--accent),
      var(--primary)
    );
    background-size: 400% 100%;
    animation: progressGradient 6s linear infinite;
    width: 0%;
    transition: width 80ms linear;
  }

  @keyframes progressGradient {
    to { background-position: 400% 0; }
  }

  /* ═══════════════════════════════════════════════════════════════
     NAVIGATION
     ═══════════════════════════════════════════════════════════════ */
  #mainNav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: var(--nav-height);
    z-index: 10000;
    background: rgba(254, 248, 240, 0.88);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 2px solid var(--card-border);
    transition: 
      background-color var(--duration-normal),
      border-color var(--duration-normal),
      box-shadow var(--duration-normal);
  }

  [data-theme="dark"] #mainNav {
    background: rgba(15, 13, 18, 0.88);
  }

  #mainNav.scrolled {
    background: var(--bg);
    box-shadow: var(--shadow-sm);
  }

  .nav-inner {
    max-width: var(--container-max);
    height: 100%;
    margin: 0 auto;
    padding: 0 var(--space-lg);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-lg);
  }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.2rem;
    color: var(--text);
    transition: color var(--duration-fast);
  }

  .nav-logo:hover {
    color: var(--primary);
  }

  .nav-logo-icon {
    width: 40px;
    height: 40px;
    border-radius: var(--radius-sm);
    background: linear-gradient(135deg, var(--primary), var(--coral));
    border: 2.5px solid var(--text);
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 3px 3px 0 var(--text);
    transition: transform var(--duration-normal) var(--bounce);
    flex-shrink: 0;
  }

  .nav-logo:hover .nav-logo-icon {
    transform: rotate(-8deg) scale(1.08);
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
  }

  .nav-links a {
    padding: var(--space-sm) var(--space-md);
    border-radius: var(--radius-sm);
    font-size: 0.9rem;
    font-weight: 700;
    color: var(--text-muted);
    transition: all var(--duration-fast);
    border: 2px solid transparent;
  }

  .nav-links a:hover {
    color: var(--primary);
    background: var(--primary-light);
  }

  .nav-links a.active {
    color: var(--primary);
    background: var(--primary-light);
    border-color: var(--primary);
  }

  .nav-cta {
    padding: var(--space-sm) var(--space-lg) !important;
    background: var(--primary) !important;
    color: #fff !important;
    border: 2.5px solid var(--text) !important;
    border-radius: var(--radius-sm) !important;
    box-shadow: 3px 3px 0 var(--text);
    transition: all var(--duration-normal) var(--bounce) !important;
  }

  .nav-cta:hover {
    background: var(--primary-hover) !important;
    transform: translate(-2px, -2px);
    box-shadow: 5px 5px 0 var(--text) !important;
  }

  .nav-cta:active {
    transform: translate(1px, 1px);
    box-shadow: 2px 2px 0 var(--text) !important;
  }

  /* Theme Toggle */
  .theme-toggle {
    width: 44px;
    height: 44px;
    border-radius: var(--radius-sm);
    background: var(--card);
    border: 2px solid var(--card-border);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all var(--duration-normal) var(--bounce);
    position: relative;
    overflow: hidden;
    flex-shrink: 0;
  }

  .theme-toggle:hover {
    transform: scale(1.08) rotate(8deg);
    border-color: var(--accent);
    box-shadow: var(--shadow-sm);
  }

  .theme-toggle .icon-sun,
  .theme-toggle .icon-moon {
    position: absolute;
    transition: all var(--duration-slow) var(--bounce);
  }

  .theme-toggle .icon-sun {
    opacity: 1;
    transform: scale(1) rotate(0deg);
    color: var(--accent);
  }

  .theme-toggle .icon-moon {
    opacity: 0;
    transform: scale(0.4) rotate(-90deg);
    color: var(--lavender);
  }

  [data-theme="dark"] .theme-toggle .icon-sun {
    opacity: 0;
    transform: scale(0.4) rotate(90deg);
  }

  [data-theme="dark"] .theme-toggle .icon-moon {
    opacity: 1;
    transform: scale(1) rotate(0deg);
  }

  /* Hamburger Menu */
  .hamburger {
    display: none;
    width: 44px;
    height: 44px;
    border-radius: var(--radius-sm);
    background: var(--card);
    border: 2px solid var(--card-border);
    cursor: pointer;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 5px;
    flex-shrink: 0;
    transition: all var(--duration-normal);
  }

  .hamburger:hover {
    border-color: var(--primary);
  }

  .hamburger span {
    display: block;
    width: 20px;
    height: 2.5px;
    background: var(--text);
    border-radius: 2px;
    transition: all var(--duration-normal);
    transform-origin: center;
  }

  .hamburger.open span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
  }

  .hamburger.open span:nth-child(2) {
    opacity: 0;
    transform: scaleX(0);
  }

  .hamburger.open span:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px);
  }

  /* Mobile Menu */
  .mobile-menu {
    display: none;
    position: fixed;
    top: var(--nav-height);
    left: 0;
    right: 0;
    background: var(--bg);
    border-bottom: 3px solid var(--card-border);
    padding: var(--space-md);
    z-index: 9999;
    box-shadow: var(--shadow-lg);
    transform: translateY(-12px);
    opacity: 0;
    pointer-events: none;
    transition: all var(--duration-normal) var(--bounce);
  }

  .mobile-menu.open {
    transform: translateY(0);
    opacity: 1;
    pointer-events: auto;
  }

  .mobile-menu a {
    display: block;
    padding: var(--space-md);
    border-radius: var(--radius-sm);
    font-weight: 700;
    font-size: 1rem;
    color: var(--text-muted);
    margin-bottom: var(--space-xs);
    transition: all var(--duration-fast);
  }

  .mobile-menu a:hover {
    background: var(--primary-light);
    color: var(--primary);
  }

  .mobile-menu a.mobile-cta {
    background: var(--primary);
    color: #fff;
    text-align: center;
    margin-top: var(--space-sm);
  }

  @media (max-width: 900px) {
    .nav-links { display: none !important; }
    .hamburger { display: flex; }
    .mobile-menu { display: block; }
  }

  /* ═══════════════════════════════════════════════════════════════
     DECORATIVE ELEMENTS
     ═══════════════════════════════════════════════════════════════ */
  .deco {
    position: absolute;
    pointer-events: none;
    z-index: 0;
  }

  /* Floating Clouds */
  .deco-cloud {
    width: 90px;
    height: 45px;
    background: var(--card);
    border-radius: 45px;
    border: 2.5px solid var(--card-border);
    box-shadow: 4px 4px 0 var(--card-border);
    animation: floatCloud 10s ease-in-out infinite;
    transition: background var(--duration-slow), border-color var(--duration-slow);
  }

  .deco-cloud::before,
  .deco-cloud::after {
    content: '';
    position: absolute;
    background: var(--card);
    border: 2.5px solid var(--card-border);
    border-bottom-color: transparent;
  }

  .deco-cloud::before {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    top: -22px;
    left: 16px;
  }

  .deco-cloud::after {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    top: -14px;
    left: 46px;
  }

  @keyframes floatCloud {
    0%, 100% { transform: translateY(0) translateX(0); }
    50% { transform: translateY(-15px) translateX(10px); }
  }

  /* Stars */
  .deco-star {
    width: 20px;
    height: 20px;
    background: var(--accent);
    clip-path: polygon(
      50% 0%, 61% 35%, 98% 35%, 68% 57%,
      79% 91%, 50% 70%, 21% 91%, 32% 57%,
      2% 35%, 39% 35%
    );
    animation: twinkle 3.5s ease-in-out infinite;
  }

  @keyframes twinkle {
    0%, 100% { transform: scale(1) rotate(0deg); opacity: 0.75; }
    50% { transform: scale(1.25) rotate(12deg); opacity: 1; }
  }

  /* Hearts */
  .deco-heart {
    width: 18px;
    height: 16px;
    background: var(--rose);
    transform: rotate(-45deg);
    border-radius: 0 50% 0 50%;
    animation: floatHeart 4.5s ease-in-out infinite;
  }

  .deco-heart::before,
  .deco-heart::after {
    content: '';
    position: absolute;
    background: var(--rose);
  }

  .deco-heart::before {
    width: 18px;
    height: 16px;
    border-radius: 50% 50% 0 0;
    top: -8px;
    left: 0;
  }

  @keyframes floatHeart {
    0%, 100% { transform: rotate(-45deg) translateY(0) scale(1); }
    50% { transform: rotate(-45deg) translateY(-10px) scale(1.12); }
  }

  /* Bubbles */
  .deco-bubble {
    border-radius: 50%;
    border: 2px solid var(--card-border);
    animation: floatBubble 6s ease-in-out infinite;
    transition: border-color var(--duration-slow);
  }

  @keyframes floatBubble {
    0%, 100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-20px) scale(1.05); }
  }

  /* Sparkle Burst */
  .deco-sparkle {
    width: 14px;
    height: 14px;
    background: var(--accent);
    clip-path: polygon(
      50% 0%, 55% 40%, 100% 50%,
      55% 60%, 50% 100%, 45% 60%,
      0% 50%, 45% 40%
    );
    animation: sparklePulse 2s ease-in-out infinite;
  }

  @keyframes sparklePulse {
    0%, 100% { transform: scale(1) rotate(0deg); }
    50% { transform: scale(1.4) rotate(45deg); }
  }

  /* Geometric Shapes */
  .deco-triangle {
    width: 0;
    height: 0;
    border-left: 12px solid transparent;
    border-right: 12px solid transparent;
    border-bottom: 20px solid var(--mint);
    animation: floatTriangle 5s ease-in-out infinite;
  }

  @keyframes floatTriangle {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-12px) rotate(10deg); }
  }

  .deco-diamond {
    width: 16px;
    height: 16px;
    background: var(--lavender);
    transform: rotate(45deg);
    animation: floatDiamond 4s ease-in-out infinite;
  }

  @keyframes floatDiamond {
    0%, 100% { transform: rotate(45deg) translateY(0); }
    50% { transform: rotate(45deg) translateY(-8px); }
  }

  /* ═══════════════════════════════════════════════════════════════
     REVEAL ANIMATIONS
     ═══════════════════════════════════════════════════════════════ */
  .reveal {
    opacity: 0;
    transform: translateY(35px) scale(0.96);
    transition: 
      opacity var(--duration-slower) var(--ease-out),
      transform var(--duration-slower) var(--ease-out);
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  .reveal.d-1 { transition-delay: 80ms; }
  .reveal.d-2 { transition-delay: 160ms; }
  .reveal.d-3 { transition-delay: 240ms; }
  .reveal.d-4 { transition-delay: 320ms; }
  .reveal.d-5 { transition-delay: 400ms; }
  .reveal.d-6 { transition-delay: 480ms; }
  .reveal.d-7 { transition-delay: 560ms; }
  .reveal.d-8 { transition-delay: 640ms; }

  /* ═══════════════════════════════════════════════════════════════
     BUTTONS
     ═══════════════════════════════════════════════════════════════ */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-sm);
    padding: var(--space-md) var(--space-xl);
    border-radius: var(--radius-md);
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    border: 2.5px solid var(--text);
    transition: all var(--duration-normal) var(--bounce);
    position: relative;
    overflow: hidden;
    white-space: nowrap;
  }

  .btn::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.2), transparent);
    opacity: 0;
    transition: opacity var(--duration-fast);
  }

  .btn:hover::before {
    opacity: 1;
  }

  .btn:active {
    transform: scale(0.96) !important;
  }

  .btn-primary {
    background: var(--primary);
    color: #fff;
    box-shadow: 4px 4px 0 var(--text);
  }

  .btn-primary:hover {
    background: var(--primary-hover);
    transform: translateY(-3px);
    box-shadow: 6px 6px 0 var(--text);
  }

  .btn-secondary {
    background: var(--card);
    color: var(--text);
    box-shadow: 4px 4px 0 var(--card-border);
    transition: all var(--duration-normal) var(--bounce), background var(--duration-slow), color var(--duration-slow);
  }

  .btn-secondary:hover {
    transform: translateY(-3px);
    box-shadow: 6px 6px 0 var(--card-border-hover);
    border-color: var(--primary);
    color: var(--primary);
  }

  .btn-ghost {
    background: transparent;
    color: var(--text);
    border-color: transparent;
  }

  .btn-ghost:hover {
    background: var(--primary-light);
    color: var(--primary);
  }

  .btn-sm {
    padding: var(--space-sm) var(--space-md);
    font-size: 0.875rem;
  }

  .btn-lg {
    padding: var(--space-lg) var(--space-2xl);
    font-size: 1.125rem;
  }

  /* Icon Only Button */
  .btn-icon {
    width: 48px;
    height: 48px;
    padding: 0;
    border-radius: var(--radius-sm);
  }

  /* ═══════════════════════════════════════════════════════════════
     BADGES & LABELS
     ═══════════════════════════════════════════════════════════════ */
  .badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-xs);
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border: 2px solid;
    transition: all var(--duration-slow);
  }

  .badge-primary {
    color: var(--primary);
    background: var(--primary-light);
    border-color: var(--primary);
  }

  .badge-secondary {
    color: var(--secondary);
    background: var(--secondary-light);
    border-color: var(--secondary);
  }

  .badge-tertiary {
    color: var(--tertiary);
    background: var(--tertiary-light);
    border-color: var(--tertiary);
  }

  .badge-accent {
    color: var(--accent-dark);
    background: var(--accent-light);
    border-color: var(--accent);
  }

  .badge-rose {
    color: var(--rose);
    background: var(--rose-light);
    border-color: var(--rose);
  }

  .badge-mint {
    color: var(--mint);
    background: var(--mint-light);
    border-color: var(--mint);
  }

  .badge-lavender {
    color: var(--lavender);
    background: var(--lavender-light);
    border-color: var(--lavender);
  }

  /* Section Label */
  .section-label {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-sm) var(--space-lg);
    border-radius: var(--radius-full);
    font-size: 0.75rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    border: 2px solid;
    margin-bottom: var(--space-md);
    transition: all var(--duration-slow);
  }

  /* Live Dot */
  .live-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--success);
    border: 2px solid var(--text);
    animation: pulse 2s ease-in-out infinite;
    flex-shrink: 0;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(102, 187, 106, 0.4); }
    50% { transform: scale(1.15); box-shadow: 0 0 0 6px rgba(102, 187, 106, 0); }
  }

  /* ═══════════════════════════════════════════════════════════════
     HERO SECTION
     ═══════════════════════════════════════════════════════════════ */
  .hero-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: calc(var(--nav-height) + var(--space-2xl)) var(--space-lg) var(--space-4xl);
    overflow: hidden;
  }

  /* Background Blobs */
  .hero-blob {
    position: absolute;
    border-radius: 50%;
    pointer-events: none;
    filter: blur(100px);
    opacity: 0.35;
    transition: background var(--duration-slow);
  }

  .hero-blob-1 {
    width: 600px;
    height: 600px;
    background: var(--primary);
    top: -15%;
    right: -10%;
  }

  .hero-blob-2 {
    width: 500px;
    height: 500px;
    background: var(--secondary);
    bottom: -10%;
    left: -8%;
  }

  .hero-blob-3 {
    width: 300px;
    height: 300px;
    background: var(--tertiary);
    top: 45%;
    left: 55%;
    opacity: 0.2;
  }

  .hero-blob-4 {
    width: 200px;
    height: 200px;
    background: var(--accent);
    bottom: 20%;
    right: 20%;
    opacity: 0.25;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-sm) var(--space-lg);
    border-radius: var(--radius-full);
    background: var(--card);
    border: 2.5px solid var(--card-border);
    box-shadow: 3px 3px 0 var(--card-border);
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--text-muted);
    margin-bottom: var(--space-xl);
    transition: all var(--duration-slow);
  }

  .hero-title {
    font-size: clamp(2.5rem, 7vw, 4.5rem);
    font-weight: 700;
    color: var(--text);
    margin-bottom: var(--space-md);
    line-height: 1.1;
    transition: color var(--duration-slow);
  }

  .hero-title-accent {
    color: var(--primary);
    position: relative;
    display: inline-block;
    transition: color var(--duration-slow);
  }

  .hero-title-accent::after {
    content: '';
    position: absolute;
    bottom: 0.1em;
    left: 0;
    right: 0;
    height: 0.18em;
    background: var(--primary-light);
    border-radius: var(--radius-full);
    z-index: -1;
    transition: background var(--duration-slow);
  }

  .hero-subtitle {
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    color: var(--text-muted);
    line-height: 1.8;
    max-width: 600px;
    margin: 0 auto var(--space-2xl);
    transition: color var(--duration-slow);
  }

  .hero-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-md);
    justify-content: center;
    margin-bottom: var(--space-lg);
  }

  .hero-hint {
    font-size: 0.8rem;
    color: var(--text-light);
    font-weight: 600;
    transition: color var(--duration-slow);
  }

  .hero-hint kbd {
    display: inline-block;
    padding: 0.15rem 0.5rem;
    background: var(--card);
    border: 2px solid var(--card-border);
    border-radius: var(--radius-xs);
    font-family: var(--font-mono);
    font-size: 0.75rem;
    box-shadow: 2px 2px 0 var(--card-border);
    color: var(--text-muted);
    margin: 0 0.15rem;
  }

  /* Floating Preview Windows */
  .preview-window {
    position: absolute;
    border-radius: var(--radius-md);
    overflow: hidden;
    border: 3px solid var(--card-border);
    box-shadow: 6px 6px 0 var(--card-border);
    transition: transform var(--duration-slow) var(--bounce), border-color var(--duration-slow);
    backdrop-filter: blur(4px);
  }

  .preview-window:hover {
    transform: rotate(0deg) scale(1.05) !important;
    border-color: var(--primary);
  }

  .pw-titlebar {
    height: 26px;
    background: rgba(0, 0, 0, 0.04);
    border-bottom: 2px solid var(--card-border);
    display: flex;
    align-items: center;
    padding: 0 var(--space-sm);
    gap: 4px;
    transition: background var(--duration-slow), border-color var(--duration-slow);
  }

  [data-theme="dark"] .pw-titlebar {
    background: rgba(0, 0, 0, 0.25);
  }

  .pw-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    border: 1.5px solid var(--text);
    flex-shrink: 0;
  }

  .pw-dot.close { background: #EF5350; }
  .pw-dot.minimize { background: #FFCA28; }
  .pw-dot.maximize { background: #66BB6A; }

  .pw-content {
    transition: background var(--duration-slow);
  }

  /* Scroll Indicator */
  .scroll-indicator {
    position: absolute;
    bottom: var(--space-2xl);
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
    animation: scrollBounce 2.5s ease-in-out infinite;
    color: var(--text-light);
    font-size: 0.7rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.15em;
  }

  @keyframes scrollBounce {
    0%, 100% { transform: translateX(-50%) translateY(0); opacity: 1; }
    50% { transform: translateX(-50%) translateY(12px); opacity: 0.5; }
  }

  /* ═══════════════════════════════════════════════════════════════
     CAROUSEL
     ═══════════════════════════════════════════════════════════════ */
  .carousel-wrapper {
    position: relative;
    border-radius: var(--radius-xl);
    overflow: hidden;
    border: 3px solid var(--card-border);
    box-shadow: 6px 6px 0 var(--card-border);
    background: var(--card);
    transition: all var(--duration-slow);
  }

  .carousel-wrapper:hover {
    border-color: var(--card-border-hover);
  }

  .carousel-viewport {
    overflow: hidden;
    position: relative;
    aspect-ratio: 16/9;
    background: var(--bg-alt);
    transition: background var(--duration-slow);
  }

  .carousel-track {
    display: flex;
    height: 100%;
    transition: transform var(--duration-slow) var(--bounce);
  }

  .carousel-slide {
    min-width: 100%;
    height: 100%;
    position: relative;
  }

  .carousel-slide iframe {
    position: absolute;
    left: 50%;
    top: 50%;
    width: 1280px;
    height: 720px;
    margin-left: -640px;
    margin-top: -360px;
    transform: scale(0.3125);
    transform-origin: center center;
    border: none;
    pointer-events: none;
  }

  .carousel-controls {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: var(--space-md);
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 5;
  }

  .carousel-controls::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0,0,0,0.35), transparent);
    pointer-events: none;
    z-index: -1;
  }

  .carousel-btn {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--card);
    border: 2.5px solid var(--text);
    box-shadow: 2px 2px 0 var(--text);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text);
    transition: all var(--duration-fast) var(--bounce);
    flex-shrink: 0;
  }

  .carousel-btn:hover {
    transform: scale(1.1);
    box-shadow: 3px 3px 0 var(--text);
    border-color: var(--primary);
    color: var(--primary);
  }

  .carousel-dots {
    display: flex;
    gap: 6px;
  }

  .carousel-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.25);
    border: 2px solid rgba(255, 255, 255, 0.5);
    cursor: pointer;
    transition: all var(--duration-normal) var(--bounce);
  }

  .carousel-dot.active {
    background: var(--primary);
    border-color: #fff;
    transform: scale(1.3);
  }

  .carousel-dot:hover:not(.active) {
    transform: scale(1.15);
    background: rgba(255, 255, 255, 0.5);
  }

  /* ═══════════════════════════════════════════════════════════════
     FEATURE CARDS
     ═══════════════════════════════════════════════════════════════ */
  .feature-card {
    background: var(--card);
    border: 2.5px solid var(--card-border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    box-shadow: var(--shadow-card);
    transition: all var(--duration-normal);
    position: relative;
    overflow: hidden;
    cursor: default;
  }

  .feature-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--card-border), transparent);
    opacity: 0;
    transition: opacity var(--duration-fast);
  }

  .feature-card:hover {
    border-color: var(--card-border-hover);
    box-shadow: var(--shadow-card-hover);
    transform: translateY(-5px);
  }

  .feature-card:hover::before {
    opacity: 1;
  }

  .feature-icon {
    width: 60px;
    height: 60px;
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2.5px solid;
    margin-bottom: var(--space-lg);
    box-shadow: 4px 4px 0 currentColor;
    flex-shrink: 0;
    transition: all var(--duration-slow);
  }

  .fc-primary {
    background: var(--primary-light);
    border-color: var(--primary);
    color: var(--primary);
  }

  .fc-secondary {
    background: var(--secondary-light);
    border-color: var(--secondary);
    color: var(--secondary);
  }

  .fc-tertiary {
    background: var(--tertiary-light);
    border-color: var(--tertiary);
    color: var(--tertiary);
  }

  .fc-accent {
    background: var(--accent-light);
    border-color: var(--accent-dark);
    color: var(--accent-dark);
  }

  .fc-rose {
    background: var(--rose-light);
    border-color: var(--rose);
    color: var(--rose);
  }

  .fc-sky {
    background: var(--sky-light);
    border-color: var(--sky);
    color: var(--sky);
  }

  .fc-mint {
    background: var(--mint-light);
    border-color: var(--mint);
    color: var(--mint);
  }

  .fc-lavender {
    background: var(--lavender-light);
    border-color: var(--lavender);
    color: var(--lavender);
  }

  .fc-lime {
    background: var(--lime-light);
    border-color: var(--lime);
    color: var(--lime);
  }

  .feature-card h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: var(--space-sm);
    color: var(--text);
    transition: color var(--duration-slow);
  }

  .feature-card p {
    font-size: 0.95rem;
    color: var(--text-muted);
    line-height: 1.7;
    transition: color var(--duration-slow);
  }

  /* ═══════════════════════════════════════════════════════════════
     STATS SECTION
     ═══════════════════════════════════════════════════════════════ */
  .stats-section {
    border-top: 3px solid var(--card-border);
    border-bottom: 3px solid var(--card-border);
    background: var(--bg-alt);
    position: relative;
    overflow: hidden;
    transition: background var(--duration-slow);
  }

  .stats-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: radial-gradient(var(--bg-dot) 1px, transparent 1px);
    background-size: 20px 20px;
    opacity: 0.4;
    pointer-events: none;
  }

  .stat-card {
    text-align: center;
    padding: var(--space-lg) var(--space-md);
    background: var(--card);
    border: 2.5px solid var(--card-border);
    border-radius: var(--radius-lg);
    box-shadow: 4px 4px 0 var(--card-border);
    transition: transform var(--duration-normal) var(--bounce);
    position: relative;
    z-index: 1;
  }

  .stat-card:hover {
    transform: translateY(-5px) rotate(-1deg);
  }

  .stat-icon {
    width: 48px;
    height: 48px;
    border-radius: var(--radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto var(--space-sm);
  }

  .stat-value {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 700;
    color: var(--primary);
    transition: color var(--duration-slow);
    font-variant-numeric: tabular-nums;
    line-height: 1;
  }

  .stat-label {
    font-size: 0.9rem;
    font-weight: 700;
    color: var(--text-muted);
    margin-top: var(--space-xs);
    transition: color var(--duration-slow);
  }

  /* ═══════════════════════════════════════════════════════════════
     COMPARISON SLIDER
     ═══════════════════════════════════════════════════════════════ */
  .comparison-container {
    position: relative;
    overflow: hidden;
    border-radius: var(--radius-xl);
    border: 3px solid var(--card-border);
    box-shadow: 6px 6px 0 var(--card-border);
    cursor: col-resize;
    user-select: none;
    aspect-ratio: 16/9;
    transition: border-color var(--duration-slow);
  }

  .comparison-container:hover {
    border-color: var(--card-border-hover);
  }

  .comp-layer {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .comp-slider-line {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 4px;
    background: var(--primary);
    z-index: 10;
    box-shadow: 0 0 0 3px var(--text);
    transition: background var(--duration-slow);
  }

  .comp-handle {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: var(--primary);
    border: 3px solid var(--text);
    box-shadow: 3px 3px 0 var(--text);
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
    transition: background var(--duration-slow);
  }

  .comp-label {
    position: absolute;
    top: var(--space-md);
    padding: var(--space-xs) var(--space-md);
    border-radius: var(--radius-full);
    font-size: 0.75rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border: 2px solid var(--text);
    box-shadow: 2px 2px 0 var(--text);
    z-index: 5;
    transition: background var(--duration-slow);
  }

  .comp-label-before {
    left: var(--space-md);
    background: var(--bg-alt);
    color: var(--text-muted);
  }

  .comp-label-after {
    right: var(--space-md);
    background: var(--primary);
    color: #fff;
  }

  /* ═══════════════════════════════════════════════════════════════
     INSTALLATION STEPS
     ═══════════════════════════════════════════════════════════════ */
  .steps-container {
    position: relative;
  }

  .step-connector {
    width: 4px;
    height: 40px;
    margin: var(--space-xs) auto;
    background: var(--card-border);
    border-radius: 4px;
    position: relative;
    overflow: hidden;
    transition: background var(--duration-slow);
  }

  .step-connector-fill {
    position: absolute;
    inset: 0;
    background: var(--primary);
    border-radius: 4px;
    transform: scaleY(0);
    transform-origin: top;
    transition: transform 1s var(--ease-out);
  }

  .step-item.completed .step-connector-fill {
    transform: scaleY(1);
  }

  .step-number {
    width: 52px;
    height: 52px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.3rem;
    background: var(--card);
    border: 3px solid var(--card-border);
    color: var(--text-muted);
    box-shadow: 4px 4px 0 var(--card-border);
    transition: all var(--duration-slow) var(--bounce);
    flex-shrink: 0;
  }

  .step-item.active .step-number {
    background: var(--primary);
    border-color: var(--text);
    color: #fff;
    box-shadow: 4px 4px 0 var(--text);
    transform: scale(1.1) rotate(5deg);
  }

  .step-item.completed .step-number {
    background: var(--success);
    border-color: var(--text);
    color: #fff;
    box-shadow: 4px 4px 0 var(--text);
  }

  .step-content {
    flex: 1;
    padding-bottom: var(--space-xl);
  }

  .step-title {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1.15rem;
    color: var(--text);
    margin-bottom: var(--space-xs);
    transition: color var(--duration-slow);
  }

  .step-desc {
    font-size: 0.95rem;
    color: var(--text-muted);
    margin-bottom: var(--space-md);
    transition: color var(--duration-slow);
  }

  /* Code Block */
  .code-block {
    background: #1A1A2E;
    border: 2.5px solid var(--card-border);
    border-radius: var(--radius-sm);
    padding: var(--space-md) var(--space-lg);
    font-size: 0.85rem;
    color: #A5D6FF;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
    box-shadow: 3px 3px 0 var(--card-border);
    transition: border-color var(--duration-fast);
    overflow-x: auto;
  }

  .code-block:hover {
    border-color: var(--primary);
  }

  .code-block code {
    font-family: var(--font-mono);
  }

  .code-keyword { color: #FF79C6; font-weight: 600; }
  .code-string { color: #F1FA8C; }
  .code-func { color: #8BE9FD; }
  .code-comment { color: #6272A4; font-style: italic; }
  .code-number { color: #BD93F9; }

  .copy-btn {
    padding: var(--space-xs) var(--space-md);
    border-radius: var(--radius-xs);
    background: var(--card);
    border: 2px solid var(--card-border);
    color: var(--text-muted);
    font-size: 0.75rem;
    font-weight: 800;
    font-family: var(--font-body);
    cursor: pointer;
    transition: all var(--duration-fast) var(--bounce);
    box-shadow: 2px 2px 0 var(--card-border);
    white-space: nowrap;
    flex-shrink: 0;
  }

  .copy-btn:hover {
    border-color: var(--primary);
    color: var(--primary);
    transform: translate(-1px, -1px);
  }

  .copy-btn:active {
    transform: translate(1px, 1px);
  }

  .copy-btn.copied {
    background: var(--success-light);
    border-color: var(--success);
    color: var(--success);
  }

  /* ═══════════════════════════════════════════════════════════════
     FAQ SECTION
     ═══════════════════════════════════════════════════════════════ */
  .faq-item {
    border: 2.5px solid var(--card-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: var(--card);
    box-shadow: 4px 4px 0 var(--card-border);
    transition: all var(--duration-normal) var(--bounce);
    cursor: pointer;
  }

  .faq-item:hover {
    border-color: var(--card-border-hover);
  }

  .faq-item.open {
    border-color: var(--primary);
    box-shadow: 4px 4px 0 var(--primary);
  }

  .faq-question {
    padding: var(--space-lg);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1.05rem;
    color: var(--text);
    transition: color var(--duration-fast);
    gap: var(--space-md);
  }

  .faq-item.open .faq-question {
    color: var(--primary);
  }

  .faq-chevron {
    transition: transform var(--duration-slow) var(--bounce);
    color: var(--text-light);
    flex-shrink: 0;
  }

  .faq-item.open .faq-chevron {
    transform: rotate(180deg);
    color: var(--primary);
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height var(--duration-slow) var(--ease-out), padding var(--duration-normal);
    padding: 0 var(--space-lg);
    cursor: default;
  }

  .faq-item.open .faq-answer {
    max-height: 400px;
    padding: 0 var(--space-lg) var(--space-lg);
  }

  .faq-answer p {
    font-size: 0.95rem;
    color: var(--text-muted);
    line-height: 1.8;
  }

  .faq-answer code {
    background: var(--primary-light);
    color: var(--primary);
    padding: 0.1rem 0.4rem;
    border-radius: var(--radius-xs);
    font-size: 0.85rem;
    font-weight: 600;
    border: 1.5px solid var(--primary);
  }

  .faq-answer strong {
    color: var(--text);
  }

  .faq-answer ul {
    margin: var(--space-sm) 0;
    padding-left: var(--space-lg);
  }

  .faq-answer li {
    font-size: 0.95rem;
    color: var(--text-muted);
    margin-bottom: var(--space-xs);
  }

  /* ═══════════════════════════════════════════════════════════════
     CTA SECTION
     ═══════════════════════════════════════════════════════════════ */
  .cta-section {
    position: relative;
    overflow: hidden;
    padding: var(--space-4xl) var(--space-lg);
    background: var(--bg-alt);
    border-top: 3px solid var(--card-border);
    transition: background var(--duration-slow), border-color var(--duration-slow);
  }

  .cta-inner {
    position: relative;
    z-index: 2;
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .cta-icon {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: var(--primary-light);
    border: 3px solid var(--primary);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto var(--space-lg);
    box-shadow: 4px 4px 0 var(--primary);
  }

  .cta-section h2 {
    font-size: clamp(1.75rem, 4vw, 2.75rem);
    font-weight: 700;
    color: var(--text);
    margin-bottom: var(--space-md);
    transition: color var(--duration-slow);
  }

  .cta-section p {
    font-size: 1.1rem;
    color: var(--text-muted);
    line-height: 1.8;
    margin-bottom: var(--space-xl);
    transition: color var(--duration-slow);
  }

  /* ═══════════════════════════════════════════════════════════════
     FOOTER
     ═══════════════════════════════════════════════════════════════ */
  .site-footer {
    border-top: 3px solid var(--card-border);
    padding: var(--space-xl) var(--space-lg);
    background: var(--bg);
    transition: background var(--duration-slow), border-color var(--duration-slow);
  }

  .footer-inner {
    max-width: var(--container-max);
    margin: 0 auto;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
  }

  .footer-logo {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
  }

  .footer-logo-icon {
    width: 32px;
    height: 32px;
    border-radius: var(--radius-xs);
    background: var(--primary);
    border: 2px solid var(--text);
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 2px 2px 0 var(--text);
  }

  .footer-logo span {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 0.95rem;
    color: var(--text-muted);
  }

  .footer-links {
    display: flex;
    gap: var(--space-lg);
  }

  .footer-links a {
    font-size: 0.9rem;
    font-weight: 700;
    color: var(--text-light);
    transition: color var(--duration-fast);
  }

  .footer-links a:hover {
    color: var(--primary);
  }

  .footer-copy {
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--text-light);
  }

  /* ═══════════════════════════════════════════════════════════════
     BACK TO TOP BUTTON
     ═══════════════════════════════════════════════════════════════ */
  #backToTop {
    position: fixed;
    bottom: var(--space-lg);
    right: var(--space-lg);
    z-index: 9990;
    width: 52px;
    height: 52px;
    border-radius: 50%;
    background: var(--primary);
    border: 2.5px solid var(--text);
    box-shadow: 3px 3px 0 var(--text);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
    transition: all var(--duration-normal) var(--bounce), background var(--duration-slow);
    opacity: 0;
    transform: translateY(20px) scale(0.85);
    pointer-events: none;
  }

  #backToTop.visible {
    opacity: 1;
    transform: translateY(0) scale(1);
    pointer-events: auto;
  }

  #backToTop:hover {
    transform: translateY(-4px) scale(1.1);
    box-shadow: 5px 5px 0 var(--text);
  }

  /* ═══════════════════════════════════════════════════════════════
     TOAST NOTIFICATIONS
     ═══════════════════════════════════════════════════════════════ */
  .toast-container {
    position: fixed;
    bottom: 6rem;
    right: var(--space-lg);
    z-index: 99999;
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    pointer-events: none;
  }

  .toast {
    pointer-events: auto;
    padding: var(--space-md) var(--space-lg);
    border-radius: var(--radius-sm);
    font-size: 0.9rem;
    font-weight: 700;
    border: 2.5px solid var(--text);
    box-shadow: 3px 3px 0 var(--text);
    animation: toastIn 0.4s var(--bounce), toastOut 0.3s ease 2.2s forwards;
  }

  .toast.success {
    background: var(--success);
    color: #fff;
    border-color: #2E7D32;
    box-shadow: 3px 3px 0 #2E7D32;
  }

  .toast.info {
    background: var(--info);
    color: #fff;
    border-color: #0288D1;
    box-shadow: 3px 3px 0 #0288D1;
  }

  .toast.warning {
    background: var(--warning);
    color: var(--text);
    border-color: #EF6C00;
    box-shadow: 3px 3px 0 #EF6C00;
  }

  @keyframes toastIn {
    from {
      opacity: 0;
      transform: translateY(1rem) scale(0.9);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  @keyframes toastOut {
    to {
      opacity: 0;
      transform: translateY(-0.5rem) scale(0.9);
    }
  }

  /* ═══════════════════════════════════════════════════════════════
     CURSOR SPARKLE EFFECT
     ═══════════════════════════════════════════════════════════════ */
  .sparkle {
    position: fixed;
    pointer-events: none;
    z-index: 99997;
    width: 10px;
    height: 10px;
    clip-path: polygon(
      50% 0%, 61% 35%, 98% 35%, 68% 57%,
      79% 91%, 50% 70%, 21% 91%, 32% 57%,
      2% 35%, 39% 35%
    );
    animation: sparkleFade 0.6s ease-out forwards;
  }

  @keyframes sparkleFade {
    0% { opacity: 1; transform: scale(1) rotate(0); }
    100% { opacity: 0; transform: scale(0) rotate(90deg); }
  }

  /* ═══════════════════════════════════════════════════════════════
     CONFETTI EFFECT
     ═══════════════════════════════════════════════════════════════ */
  @keyframes confettiBurst {
    0% {
      opacity: 1;
      transform: translate(0, 0) rotate(0) scale(1);
    }
    100% {
      opacity: 0;
      transform: translate(var(--cx), var(--cy)) rotate(var(--cr)) scale(0.15);
    }
  }

  .confetti-piece {
    position: fixed;
    pointer-events: none;
    z-index: 99998;
    animation: confettiBurst 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
  }

  /* ═══════════════════════════════════════════════════════════════
     MASCOT
     ═══════════════════════════════════════════════════════════════ */
  #mascot {
    position: fixed;
    bottom: var(--space-lg);
    left: var(--space-lg);
    z-index: 9989;
    width: 60px;
    height: 60px;
    border-radius: 50% 50% 50% 45%;
    background: var(--primary);
    border: 3px solid var(--text);
    box-shadow: 4px 4px 0 var(--text);
    cursor: pointer;
    overflow: hidden;
    transition: transform var(--duration-slow) var(--bounce), background var(--duration-slow);
  }

  #mascot:hover {
    transform: scale(1.15) rotate(-5deg);
  }

  #mascot .eye {
    position: absolute;
    width: 9px;
    height: 11px;
    background: var(--text);
    border-radius: 50%;
    top: 18px;
  }

  #mascot .eye.left { left: 14px; }
  #mascot .eye.right { right: 14px; }

  #mascot .eye::after {
    content: '';
    position: absolute;
    width: 4px;
    height: 4px;
    background: #fff;
    border-radius: 50%;
    top: 1px;
    right: 0;
  }

  #mascot .mouth {
    position: absolute;
    bottom: 14px;
    left: 50%;
    transform: translateX(-50%);
    width: 12px;
    height: 6px;
    border-bottom: 3px solid var(--text);
    border-radius: 0 0 50% 50%;
  }

  #mascot .blush {
    position: absolute;
    width: 12px;
    height: 6px;
    background: var(--rose);
    border-radius: 50%;
    top: 32px;
    opacity: 0.5;
  }

  #mascot .blush.left { left: 4px; }
  #mascot .blush.right { right: 4px; }

  #mascot.bounce {
    animation: mascotBounce 0.6s var(--bounce);
  }

  @keyframes mascotBounce {
    0% { transform: scale(1); }
    30% { transform: scale(1.25) rotate(12deg); }
    60% { transform: scale(0.95) rotate(-5deg); }
    100% { transform: scale(1) rotate(0); }
  }

  #mascotTip {
    position: fixed;
    bottom: 5rem;
    left: var(--space-lg);
    z-index: 9989;
    background: var(--card);
    border: 2.5px solid var(--card-border);
    border-radius: var(--radius-sm);
    padding: var(--space-sm) var(--space-md);
    font-size: 0.8rem;
    font-weight: 700;
    color: var(--text-muted);
    box-shadow: 3px 3px 0 var(--card-border);
    opacity: 0;
    transform: translateY(8px);
    pointer-events: none;
    transition: all var(--duration-normal) var(--bounce);
    white-space: nowrap;
    transition: background var(--duration-slow), border-color var(--duration-slow);
  }

  #mascotTip.show {
    opacity: 1;
    transform: translateY(0);
  }

  #mascotTip::after {
    content: '';
    position: absolute;
    bottom: -7px;
    left: 1.2rem;
    width: 12px;
    height: 12px;
    background: var(--card);
    border-right: 2.5px solid var(--card-border);
    border-bottom: 2.5px solid var(--card-border);
    transform: rotate(45deg);
  }

  /* ═══════════════════════════════════════════════════════════════
     RESPONSIVE STYLES
     ═══════════════════════════════════════════════════════════════ */
  @media (max-width: 768px) {
    .preview-window { display: none !important; }
    .hero-buttons { flex-direction: column; align-items: center; }
    .btn { width: 100%; max-width: 300px; justify-content: center; }
    #mascot, #mascotTip { display: none; }
    
    .stat-card {
      padding: var(--space-md);
    }
    
    .feature-card {
      padding: var(--space-lg);
    }
  }

  @media (min-width: 769px) and (max-width: 1024px) {
    #mascot, #mascotTip { display: none; }
  }

  /* ═══════════════════════════════════════════════════════════════
     REDUCED MOTION
     ═══════════════════════════════════════════════════════════════ */
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.15ms !important;
    }
    .sparkle, .confetti-piece { display: none !important; }
  }

  /* ═══════════════════════════════════════════════════════════════
     CUSTOM SCROLLBAR
     ═══════════════════════════════════════════════════════════════ */
  ::-webkit-scrollbar {
    width: 10px;
  }

  ::-webkit-scrollbar-track {
    background: var(--bg);
  }

  ::-webkit-scrollbar-thumb {
    background: var(--card-border);
    border-radius: 5px;
    border: 2px solid var(--bg);
  }

  ::-webkit-scrollbar-thumb:hover {
    background: var(--primary);
  }

  /* ═══════════════════════════════════════════════════════════════
     CATEGORY CARDS
     ═══════════════════════════════════════════════════════════════ */
  .category-card {
    background: var(--card);
    border: 2.5px solid var(--card-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-card);
    transition: all var(--duration-normal);
    cursor: pointer;
  }

  .category-card:hover {
    border-color: var(--card-border-hover);
    box-shadow: var(--shadow-card-hover);
    transform: translateY(-5px);
  }

  .category-preview {
    aspect-ratio: 16/10;
    background: var(--bg-alt);
    position: relative;
    overflow: hidden;
  }

  .category-info {
    padding: var(--space-lg);
  }

  .category-title {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1.1rem;
    color: var(--text);
    margin-bottom: var(--space-xs);
  }

  .category-count {
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  /* ═══════════════════════════════════════════════════════════════
     TESTIMONIAL CARDS
     ═══════════════════════════════════════════════════════════════ */
  .testimonial-card {
    background: var(--card);
    border: 2.5px solid var(--card-border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    box-shadow: var(--shadow-card);
    transition: all var(--duration-normal);
  }

  .testimonial-card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-card-hover);
  }

  .testimonial-quote {
    font-size: 1rem;
    color: var(--text-muted);
    line-height: 1.8;
    margin-bottom: var(--space-lg);
    font-style: italic;
  }

  .testimonial-author {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
  }

  .testimonial-avatar {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: var(--primary-light);
    border: 2px solid var(--card-border);
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    color: var(--primary);
    font-size: 1rem;
  }

  .testimonial-name {
    font-weight: 700;
    font-size: 0.95rem;
    color: var(--text);
  }

  .testimonial-role {
    font-size: 0.85rem;
    color: var(--text-light);
  }

  /* ═══════════════════════════════════════════════════════════════
     SYSTEM REQUIREMENTS TABLE
     ═══════════════════════════════════════════════════════════════ */
  .requirements-table {
    background: var(--card);
    border: 2.5px solid var(--card-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-card);
  }

  .requirements-row {
    display: grid;
    grid-template-columns: 200px 1fr;
    border-bottom: 2px solid var(--card-border);
    transition: background var(--duration-fast);
  }

  .requirements-row:last-child {
    border-bottom: none;
  }

  .requirements-row:hover {
    background: var(--bg-alt);
  }

  .requirements-label {
    padding: var(--space-md) var(--space-lg);
    font-weight: 700;
    color: var(--text-soft);
    background: var(--bg-alt);
    border-right: 2px solid var(--card-border);
  }

  .requirements-value {
    padding: var(--space-md) var(--space-lg);
    color: var(--text-muted);
  }

  @media (max-width: 640px) {
    .requirements-row {
      grid-template-columns: 1fr;
    }
    .requirements-label {
      border-right: none;
      border-bottom: 2px solid var(--card-border);
    }
  }
</style>
{% endraw %}

<!-- Skip Link -->
<a href="#main" class="skip-link">Skip to main content</a>

<!-- Scroll Progress -->
<div id="scrollProgress"></div>

<!-- ═══════════════════════════════════════════════════════════════
     NAVIGATION
     ═══════════════════════════════════════════════════════════════ -->
<nav id="mainNav" aria-label="Main navigation">
  <div class="nav-inner">
    <a href="/" class="nav-logo" aria-label="HTMLWallarper Home">
      <div class="nav-logo-icon">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
          <circle cx="8.5" cy="8.5" r="1.5"/>
          <polyline points="21 15 16 10 5 21"/>
        </svg>
      </div>
      <span>HTMLWallarper</span>
    </a>
    
    <div class="nav-links">
      <a href="#preview">Preview</a>
      <a href="#features">Features</a>
      <a href="#categories">Categories</a>
      <a href="#install">Install</a>
      <a href="#faq">FAQ</a>
      <a href="/project/" class="nav-cta">Browse Gallery</a>
    </div>
    
    <div class="flex items-center gap-sm">
      <button class="theme-toggle" id="themeToggle" aria-label="Toggle dark mode">
        <svg class="icon-sun" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round">
          <circle cx="12" cy="12" r="4"/>
          <path d="M12 2v2m0 16v2M4.93 4.93l1.41 1.41m11.32 11.32l1.41 1.41M2 12h2m16 0h2M4.93 19.07l1.41-1.41m11.32-11.32l1.41-1.41"/>
        </svg>
        <svg class="icon-moon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round">
          <path d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z"/>
        </svg>
      </button>
      <button class="hamburger" id="hamburgerBtn" aria-label="Open menu" aria-expanded="false">
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>
</nav>

<!-- Mobile Menu -->
<div class="mobile-menu" id="mobileMenu" aria-hidden="true">
  <a href="#preview">Preview</a>
  <a href="#features">Features</a>
  <a href="#categories">Categories</a>
  <a href="#install">Install</a>
  <a href="#faq">FAQ</a>
  <a href="/project/" class="mobile-cta">Browse Gallery</a>
</div>

<main id="main">
  <!-- ═══════════════════════════════════════════════════════════════
       HERO SECTION
       ═══════════════════════════════════════════════════════════════ -->
  <section class="hero-section" id="hero">
    <!-- Background Blobs -->
    <div class="hero-blob hero-blob-1"></div>
    <div class="hero-blob hero-blob-2"></div>
    <div class="hero-blob hero-blob-3"></div>
    <div class="hero-blob hero-blob-4"></div>

    <!-- Decorative Elements -->
    <div class="deco deco-cloud" style="top: 12%; left: 3%;"></div>
    <div class="deco deco-cloud" style="top: 25%; right: 2%; width: 70px; height: 35px; animation-delay: 2.5s; opacity: 0.5;"></div>
    <div class="deco deco-star" style="top: 15%; left: 18%; animation-delay: 0.3s;"></div>
    <div class="deco deco-star" style="top: 32%; right: 14%; width: 16px; height: 16px; background: var(--rose); animation-delay: 1.8s;"></div>
    <div class="deco deco-star" style="bottom: 22%; left: 6%; width: 18px; height: 18px; background: var(--secondary); animation-delay: 2.8s;"></div>
    <div class="deco deco-heart" style="top: 28%; right: 20%; animation-delay: 1.2s;"></div>
    <div class="deco deco-heart" style="bottom: 28%; right: 4%; animation-delay: 3.5s;"></div>
    <div class="deco deco-bubble" style="top: 18%; right: 26%; width: 18px; height: 18px; animation-delay: 0.9s;"></div>
    <div class="deco deco-bubble" style="bottom: 18%; left: 20%; width: 14px; height: 14px; animation-delay: 2.4s;"></div>
    <div class="deco deco-triangle" style="top: 60%; left: 2%; animation-delay: 1.6s;"></div>
    <div class="deco deco-diamond" style="top: 45%; right: 8%; animation-delay: 2.1s;"></div>
    <div class="deco deco-sparkle" style="top: 8%; left: 50%; animation-delay: 0.6s;"></div>

    <!-- Floating Preview Windows -->
    <div class="preview-window" style="top: 10%; right: 3%; width: 260px; height: 146px; transform: rotate(4deg);">
      <div class="pw-titlebar">
        <span class="pw-dot close"></span>
        <span class="pw-dot minimize"></span>
        <span class="pw-dot maximize"></span>
      </div>
      <div class="pw-content" style="width: 100%; height: calc(100% - 26px); background: linear-gradient(135deg, var(--primary-light), var(--secondary-light)); display: flex; align-items: center; justify-content: center;">
        <div style="width: 50px; height: 50px; border-radius: 50%; border: 3px solid var(--primary); animation: ping 2s ease-in-out infinite; opacity: 0.4;"></div>
        <div style="width: 50px; height: 50px; border-radius: 50%; border: 3px solid var(--secondary); position: absolute; animation: spin 4s linear infinite; opacity: 0.3;"></div>
      </div>
    </div>

    <div class="preview-window" style="bottom: 12%; left: 2%; width: 200px; height: 112px; transform: rotate(-3deg);">
      <div class="pw-titlebar">
        <span class="pw-dot close"></span>
        <span class="pw-dot minimize"></span>
        <span class="pw-dot maximize"></span>
      </div>
      <div class="pw-content" style="width: 100%; height: calc(100% - 26px); background: linear-gradient(135deg, var(--tertiary-light), var(--rose-light)); display: flex; align-items: center; justify-content: center;">
        <div style="display: flex; flex-direction: column; gap: 4px;">
          <div style="width: 50px; height: 5px; background: var(--tertiary); border-radius: 3px; opacity: 0.4;"></div>
          <div style="width: 35px; height: 5px; background: var(--tertiary); border-radius: 3px; opacity: 0.3;"></div>
          <div style="width: 60px; height: 5px; background: var(--tertiary); border-radius: 3px; opacity: 0.35;"></div>
        </div>
      </div>
    </div>

    <div class="preview-window" style="top: 22%; left: 5%; width: 160px; height: 90px; transform: rotate(6deg); opacity: 0.55;">
      <div class="pw-titlebar">
        <span class="pw-dot close"></span>
        <span class="pw-dot minimize"></span>
        <span class="pw-dot maximize"></span>
      </div>
      <div class="pw-content" style="width: 100%; height: calc(100% - 26px); background: linear-gradient(135deg, var(--mint-light), var(--sky-light));"></div>
    </div>

    <!-- Hero Content -->
    <div class="hero-content">
      <div class="reveal hero-badge">
        <span class="live-dot"></span>
        Open Source &bull; KDE Plasma 6 Ready &bull; Zero CPU
      </div>
      
      <h1 class="reveal d-1 hero-title">
        Transform Your Desktop<br>
        <span class="hero-title-accent">Into Living Art</span>
      </h1>
      
      <p class="reveal d-2 hero-subtitle">
        A curated collection of lightweight, animated HTML wallpapers for KDE Plasma. 
        Pure CSS animations, zero JavaScript overhead, fully customizable. 
        Your desktop deserves better than static images.
      </p>
      
      <div class="reveal d-3 hero-buttons">
        <a href="/project/" class="btn btn-primary btn-lg confetti-trigger">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
            <circle cx="8.5" cy="8.5" r="1.5"/>
            <polyline points="21 15 16 10 5 21"/>
          </svg>
          Explore Gallery
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
            <path d="M13 7l5 5m0 0l-5 5m5-5H6"/>
          </svg>
        </a>
        <a href="https://github.com/flessan/HTMLWallarper" target="_blank" rel="noopener noreferrer" class="btn btn-secondary btn-lg">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/>
          </svg>
          View on GitHub
        </a>
      </div>
      
      <p class="reveal d-4 hero-hint">
        Press <kbd>G</kbd> to open gallery or <kbd>T</kbd> to toggle theme
      </p>
    </div>

    <!-- Scroll Indicator -->
    <div class="scroll-indicator">
      <span>Scroll Down</span>
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 5v14M19 12l-7 7-7-7"/>
      </svg>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       PREVIEW CAROUSEL
       ═══════════════════════════════════════════════════════════════ -->
  <section class="section" id="preview">
    <div class="container">
      <div class="text-center" style="margin-bottom: var(--space-2xl);">
        <span class="reveal section-label badge-primary">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
            <polygon points="5 3 19 12 5 21 5 3"/>
          </svg>
          Live Preview
        </span>
        <h2 class="reveal d-1">See It In Action</h2>
        <p class="reveal d-2" style="max-width: 500px; margin: var(--space-sm) auto 0;">
          Each wallpaper is a standalone HTML file that renders directly in your desktop. 
          No server, no build process, just pure CSS magic.
        </p>
      </div>

      <div class="reveal d-2">
        <div class="carousel-wrapper">
          <div class="carousel-viewport">
            <div class="carousel-track" id="carouselTrack">
              {% for wp in wallpapers %}
              <div class="carousel-slide">
                <iframe src="{{ wp.htmlUrl }}" loading="lazy"></iframe>
              </div>
              {% else %}
              <div class="carousel-slide">
                <div style="position: absolute; inset: 0; background: linear-gradient(135deg, var(--primary-light), var(--secondary-light)); display: flex; align-items: center; justify-content: center;">
                  <div style="text-align: center;">
                    <div style="width: 70px; height: 70px; border-radius: var(--radius-md); background: var(--primary-light); border: 3px solid var(--primary); display: flex; align-items: center; justify-content: center; margin: 0 auto var(--space-md); box-shadow: 4px 4px 0 var(--primary);">
                      <svg width="28" height="28" style="color: var(--primary)" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
                        <circle cx="8.5" cy="8.5" r="1.5"/>
                        <polyline points="21 15 16 10 5 21"/>
                      </svg>
                    </div>
                    <p style="font-weight: 700; font-size: 1.1rem;">Wallpaper previews will appear here</p>
                    <a href="/project/" style="color: var(--primary); font-weight: 700; font-size: 1rem; margin-top: var(--space-sm); display: inline-block;">
                      Visit Gallery
                    </a>
                  </div>
                </div>
              </div>
              {% endfor %}
            </div>
          </div>
          <div class="carousel-controls">
            <div class="flex gap-sm">
              <button class="carousel-btn" onclick="carouselPrev()" aria-label="Previous slide">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M15 18l-6-6 6-6"/>
                </svg>
              </button>
              <button class="carousel-btn" onclick="carouselNext()" aria-label="Next slide">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M9 18l6-6-6-6"/>
                </svg>
              </button>
            </div>
            <div class="carousel-dots" id="carouselDots"></div>
            <button class="carousel-btn" id="carouselPlayBtn" onclick="toggleCarouselAutoplay()" aria-label="Toggle autoplay">
              <svg width="18" height="18" id="playIcon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="9"/>
                <path d="M10 8l6 4-6 4V8z"/>
              </svg>
            </button>
          </div>
        </div>
      </div>

      <!-- Quick Stats Under Carousel -->
      <div class="reveal d-3 grid grid-cols-4 gap-md" style="margin-top: var(--space-xl);">
        <div style="text-align: center; padding: var(--space-md); background: var(--card); border: 2px solid var(--card-border); border-radius: var(--radius-md);">
          <div style="font-family: var(--font-display); font-size: 1.5rem; font-weight: 700; color: var(--primary);">{{ wallpapers | length }}</div>
          <div style="font-size: 0.85rem; color: var(--text-muted); font-weight: 600;">Wallpapers</div>
        </div>
        <div style="text-align: center; padding: var(--space-md); background: var(--card); border: 2px solid var(--card-border); border-radius: var(--radius-md);">
          <div style="font-family: var(--font-display); font-size: 1.5rem; font-weight: 700; color: var(--secondary);">0%</div>
          <div style="font-size: 0.85rem; color: var(--text-muted); font-weight: 600;">CPU Usage</div>
        </div>
        <div style="text-align: center; padding: var(--space-md); background: var(--card); border: 2px solid var(--card-border); border-radius: var(--radius-md);">
          <div style="font-family: var(--font-display); font-size: 1.5rem; font-weight: 700; color: var(--tertiary);">~15KB</div>
          <div style="font-size: 0.85rem; color: var(--text-muted); font-weight: 600;">Avg Size</div>
        </div>
        <div style="text-align: center; padding: var(--space-md); background: var(--card); border: 2px solid var(--card-border); border-radius: var(--radius-md);">
          <div style="font-family: var(--font-display); font-size: 1.5rem; font-weight: 700; color: var(--accent-dark);">100%</div>
          <div style="font-size: 0.85rem; color: var(--text-muted); font-weight: 600;">Open Source</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       FEATURES SECTION
       ═══════════════════════════════════════════════════════════════ -->
  <section class="section" id="features" style="background: var(--bg-alt);">
    <div class="container">
      <div class="text-center" style="margin-bottom: var(--space-2xl);">
        <span class="reveal section-label badge-secondary">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
            <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/>
          </svg>
          Features
        </span>
        <h2 class="reveal d-1">Why HTMLWallarper?</h2>
        <p class="reveal d-2" style="max-width: 550px; margin: var(--space-sm) auto 0;">
          Built from the ground up for performance, simplicity, and customization. 
          No compromises, no bloat, just beautiful animated wallpapers.
        </p>
      </div>

      <div class="grid" style="grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: var(--space-lg);">
        <!-- Feature 1 -->
        <div class="feature-card fc-primary reveal" data-tilt>
          <div class="feature-icon fc-primary">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/>
            </svg>
          </div>
          <h3>Zero CPU Overhead</h3>
          <p>Pure static HTML with CSS animations offloaded to GPU. Your CPU stays free for what matters. Most wallpapers consume 0% CPU at idle.</p>
        </div>

        <!-- Feature 2 -->
        <div class="feature-card fc-secondary reveal d-1" data-tilt>
          <div class="feature-icon fc-secondary">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/>
            </svg>
          </div>
          <h3>Fully Customizable</h3>
          <p>Change colors, speeds, and layouts directly in the HTML file. No build tools, no compilation. Just edit and refresh. It's that simple.</p>
        </div>

        <!-- Feature 3 -->
        <div class="feature-card fc-tertiary reveal d-2" data-tilt>
          <div class="feature-icon fc-tertiary">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="2" y="3" width="20" height="14" rx="2" ry="2"/>
              <path d="M8 21h8M12 17v4"/>
            </svg>
          </div>
          <h3>CSS-Only Animations</h3>
          <p>Smooth 60fps animations using modern CSS keyframes and transforms. Hardware accelerated, battery friendly, and infinitely loopable.</p>
        </div>

        <!-- Feature 4 -->
        <div class="feature-card fc-accent reveal d-3" data-tilt>
          <div class="feature-icon fc-accent">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              <path d="M9 12l2 2 4-4"/>
            </svg>
          </div>
          <h3>No Tracking, No Ads</h3>
          <p>100% open source with zero telemetry, analytics, or ads. Your desktop is personal. We respect that. MIT licensed forever.</p>
        </div>

        <!-- Feature 5 -->
        <div class="feature-card fc-rose reveal d-4" data-tilt>
          <div class="feature-icon fc-rose">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>
            </svg>
          </div>
          <h3>Plug & Play</h3>
          <p>Download, extract, point KDE to the folder. Done. No dependencies, no installation scripts, no package managers required.</p>
        </div>

        <!-- Feature 6 -->
        <div class="feature-card fc-sky reveal d-5" data-tilt>
          <div class="feature-icon fc-sky">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
          </div>
          <h3>Community Driven</h3>
          <p>Contribute your own wallpapers via Pull Request. Join a growing community of Linux enthusiasts making desktops beautiful together.</p>
        </div>

        <!-- Feature 7 -->
        <div class="feature-card fc-mint reveal d-6" data-tilt>
          <div class="feature-icon fc-mint">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="3" y="3" width="7" height="7"/>
              <rect x="14" y="3" width="7" height="7"/>
              <rect x="14" y="14" width="7" height="7"/>
              <rect x="3" y="14" width="7" height="7"/>
            </svg>
          </div>
          <h3>Multi-Monitor Support</h3>
          <p>Works seamlessly across multiple monitors. Each display can have its own wallpaper or span a single one across all screens.</p>
        </div>

        <!-- Feature 8 -->
        <div class="feature-card fc-lavender reveal d-7" data-tilt>
          <div class="feature-icon fc-lavender">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <path d="M12 6v6l4 2"/>
            </svg>
          </div>
          <h3>Instant Updates</h3>
          <p>Changes reflect immediately. Edit a color value, save, and see your desktop transform in real-time. No reload needed.</p>
        </div>

        <!-- Feature 9 -->
        <div class="feature-card fc-lime reveal d-8" data-tilt>
          <div class="feature-icon fc-lime">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/>
              <polyline points="3.27 6.96 12 12.01 20.73 6.96"/>
              <line x1="12" y1="22.08" x2="12" y2="12"/>
            </svg>
          </div>
          <h3>Tiny File Sizes</h3>
          <p>Most wallpapers are under 15KB. That's smaller than a typical JPEG. Download the entire collection in seconds, store it anywhere.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       CATEGORY SHOWCASE
       ═══════════════════════════════════════════════════════════════ -->
  <section class="section" id="categories">
    <div class="container">
      <div class="text-center" style="margin-bottom: var(--space-2xl);">
        <span class="reveal section-label badge-tertiary">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="3" width="7" height="7"/>
            <rect x="14" y="3" width="7" height="7"/>
            <rect x="14" y="14" width="7" height="7"/>
            <rect x="3" y="14" width="7" height="7"/>
          </svg>
          Categories
        </span>
        <h2 class="reveal d-1">Find Your Style</h2>
        <p class="reveal d-2" style="max-width: 500px; margin: var(--space-sm) auto 0;">
          From minimal gradients to mesmerizing particle systems, there's something for every taste.
        </p>
      </div>

      <div class="grid" style="grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: var(--space-lg);">
        <div class="category-card reveal" onclick="window.location.href='/project/?category=gradient'">
          <div class="category-preview" style="background: linear-gradient(135deg, #FF6B35, #F7931E, #FFD54F);">
            <div style="position: absolute; inset: 0; background: inherit; filter: blur(20px); opacity: 0.5;"></div>
          </div>
          <div class="category-info">
            <h3 class="category-title">Gradients</h3>
            <p class="category-count">Smooth color transitions that flow endlessly</p>
          </div>
        </div>

        <div class="category-card reveal d-1" onclick="window.location.href='/project/?category=particles'">
          <div class="category-preview" style="background: #0a0a0f; display: flex; align-items: center; justify-content: center;">
            <div style="display: flex; gap: 8px;">
              <div style="width: 4px; height: 4px; background: #fff; border-radius: 50%; animation: floatBubble 2s ease-in-out infinite;"></div>
              <div style="width: 6px; height: 6px; background: #fff; border-radius: 50%; animation: floatBubble 2.5s ease-in-out infinite 0.3s;"></div>
              <div style="width: 5px; height: 5px; background: #fff; border-radius: 50%; animation: floatBubble 3s ease-in-out infinite 0.6s;"></div>
              <div style="width: 4px; height: 4px; background: #fff; border-radius: 50%; animation: floatBubble 2.2s ease-in-out infinite 0.9s;"></div>
            </div>
          </div>
          <div class="category-info">
            <h3 class="category-title">Particles</h3>
            <p class="category-count">Floating dots, stars, and geometric shapes</p>
          </div>
        </div>

        <div class="category-card reveal d-2" onclick="window.location.href='/project/?category=geometric'">
          <div class="category-preview" style="background: linear-gradient(135deg, #1a1a2e, #16213e); display: flex; align-items: center; justify-content: center;">
            <div style="width: 40px; height: 40px; border: 3px solid #4DD0C4; transform: rotate(45deg); animation: spin 8s linear infinite;"></div>
          </div>
          <div class="category-info">
            <h3 class="category-title">Geometric</h3>
            <p class="category-count">Rotating shapes and morphing patterns</p>
          </div>
        </div>

        <div class="category-card reveal d-3" onclick="window.location.href='/project/?category=waves'">
          <div class="category-preview" style="background: linear-gradient(135deg, #667eea, #764ba2);">
            <svg style="position: absolute; bottom: 0; left: 0; right: 0; height: 50%; opacity: 0.3;" viewBox="0 0 1200 120" preserveAspectRatio="none">
              <path d="M0,60 C150,120 350,0 600,60 C850,120 1050,0 1200,60 L1200,120 L0,120 Z" fill="rgba(255,255,255,0.2)"/>
            </svg>
          </div>
          <div class="category-info">
            <h3 class="category-title">Waves</h3>
            <p class="category-count">Flowing lines and oceanic motions</p>
          </div>
        </div>

        <div class="category-card reveal d-4" onclick="window.location.href='/project/?category=minimal'">
          <div class="category-preview" style="background: #f5f5f5; display: flex; align-items: center; justify-content: center;">
            <div style="width: 60px; height: 2px; background: #333; border-radius: 1px;"></div>
          </div>
          <div class="category-info">
            <h3 class="category-title">Minimal</h3>
            <p class="category-count">Clean, simple, distraction-free designs</p>
          </div>
        </div>

        <div class="category-card reveal d-5" onclick="window.location.href='/project/?category=nature'">
          <div class="category-preview" style="background: linear-gradient(180deg, #87CEEB, #228B22);">
            <div style="position: absolute; bottom: 20%; left: 20%; width: 0; height: 0; border-left: 25px solid transparent; border-right: 25px solid transparent; border-bottom: 40px solid #2E7D32;"></div>
          </div>
          <div class="category-info">
            <h3 class="category-title">Nature</h3>
            <p class="category-count">Organic shapes and natural elements</p>
          </div>
        </div>
      </div>

      <div class="text-center" style="margin-top: var(--space-2xl);">
        <a href="/project/" class="reveal btn btn-secondary">
          View All Categories
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       COMPARISON SECTION
       ═══════════════════════════════════════════════════════════════ -->
  <section class="section" id="comparison" style="background: var(--bg-alt);">
    <div class="container container-narrow">
      <div class="text-center" style="margin-bottom: var(--space-2xl);">
        <span class="reveal section-label badge-accent">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
            <path d="M18 20V10M12 20V4M6 20v-6"/>
          </svg>
          Comparison
        </span>
        <h2 class="reveal d-1">Static vs Animated</h2>
        <p class="reveal d-2" style="max-width: 450px; margin: var(--space-sm) auto 0;">
          Drag the slider to see the difference. Why settle for boring when you can have beautiful?
        </p>
      </div>

      <div class="reveal d-2 comparison-container" id="compContainer">
        <!-- Before: Static -->
        <div class="comp-layer" id="compBefore" style="clip-path: inset(0 50% 0 0); background: linear-gradient(135deg, var(--bg-warm), var(--bg));">
          <div style="text-align: center;">
            <svg style="width: 80px; height: 80px; color: var(--text-light); margin: 0 auto var(--space-md); opacity: 0.4;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
              <circle cx="8.5" cy="8.5" r="1.5"/>
              <polyline points="21 15 16 10 5 21"/>
            </svg>
            <p style="font-weight: 700; font-size: 1.1rem; color: var(--text-light);">Static Wallpaper</p>
            <p style="font-size: 0.9rem; color: var(--text-light); margin-top: var(--space-xs); opacity: 0.7;">Boring. Lifeless. Uninspired.</p>
          </div>
        </div>

        <!-- After: Animated -->
        <div class="comp-layer" id="compAfter" style="background: linear-gradient(135deg, var(--primary-light), var(--secondary-light)); overflow: hidden;">
          <!-- Animated elements -->
          <div style="position: absolute; width: 100px; height: 100px; border-radius: 50%; border: 4px solid var(--primary); opacity: 0.3; animation: ping 3s ease-in-out infinite;"></div>
          <div style="position: absolute; width: 150px; height: 150px; border-radius: 50%; border: 3px solid var(--secondary); animation: spin 10s linear infinite;"></div>
          <div style="position: absolute; width: 60px; height: 60px; background: var(--tertiary); opacity: 0.2; border-radius: var(--radius-sm); transform: rotate(45deg); animation: floatDiamond 5s ease-in-out infinite;"></div>
          
          <div style="position: absolute; bottom: var(--space-xl); left: 0; right: 0; text-align: center;">
            <p style="font-weight: 700; font-size: 1.1rem; color: var(--primary);">HTMLWallarper</p>
            <p style="font-size: 0.9rem; color: var(--secondary); margin-top: var(--space-xs);">60fps CSS Animations</p>
          </div>
        </div>

        <!-- Slider -->
        <div class="comp-slider-line" id="compSlider" style="left: 50%;">
          <div class="comp-handle">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
              <path d="M8 18l4-4 4 4M8 6l4 4 4-4"/>
            </svg>
          </div>
        </div>

        <span class="comp-label comp-label-before">Before</span>
        <span class="comp-label comp-label-after">After</span>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       HOW IT WORKS
       ═══════════════════════════════════════════════════════════════ -->
  <section class="section" id="how-it-works">
    <div class="container container-narrow">
      <div class="text-center" style="margin-bottom: var(--space-2xl);">
        <span class="reveal section-label badge-mint">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"/>
            <path d="M12 16v-4M12 8h.01"/>
          </svg>
          How It Works
        </span>
        <h2 class="reveal d-1">The Magic Behind</h2>
        <p class="reveal d-2" style="max-width: 500px; margin: var(--space-sm) auto 0;">
          HTMLWallarper leverages the HTML Wallpaper plugin built into KDE Plasma to render web content as your desktop background.
        </p>
      </div>

      <div class="reveal" style="background: var(--card); border: 2.5px solid var(--card-border); border-radius: var(--radius-lg); padding: var(--space-xl); box-shadow: var(--shadow-card);">
        <div style="display: flex; align-items: flex-start; gap: var(--space-lg); margin-bottom: var(--space-lg);">
          <div style="flex-shrink: 0; width: 48px; height: 48px; background: var(--primary-light); border: 2px solid var(--primary); border-radius: var(--radius-sm); display: flex; align-items: center; justify-content: center;">
            <span style="font-weight: 700; color: var(--primary);">1</span>
          </div>
          <div>
            <h4 style="margin-bottom: var(--space-xs); color: var(--text);">HTML File</h4>
            <p style="font-size: 0.95rem;">Each wallpaper is a single <code style="background: var(--primary-light); padding: 0.1rem 0.4rem; border-radius: var(--radius-xs); font-size: 0.85rem;">index.html</code> file with embedded CSS animations.</p>
          </div>
        </div>

        <div style="display: flex; align-items: flex-start; gap: var(--space-lg); margin-bottom: var(--space-lg);">
          <div style="flex-shrink: 0; width: 48px; height: 48px; background: var(--secondary-light); border: 2px solid var(--secondary); border-radius: var(--radius-sm); display: flex; align-items: center; justify-content: center;">
            <span style="font-weight: 700; color: var(--secondary);">2</span>
          </div>
          <div>
            <h4 style="margin-bottom: var(--space-xs); color: var(--text);">KDE Rendering</h4>
            <p style="font-size: 0.95rem;">KDE Plasma's built-in HTML Wallpaper plugin renders the file using WebEngine (Chromium-based).</p>
          </div>
        </div>

        <div style="display: flex; align-items: flex-start; gap: var(--space-lg);">
          <div style="flex-shrink: 0; width: 48px; height: 48px; background: var(--tertiary-light); border: 2px solid var(--tertiary); border-radius: var(--radius-sm); display: flex; align-items: center; justify-content: center;">
            <span style="font-weight: 700; color: var(--tertiary);">3</span>
          </div>
          <div>
            <h4 style="margin-bottom: var(--space-xs); color: var(--text);">GPU Acceleration</h4>
            <p style="font-size: 0.95rem;">CSS animations are hardware-accelerated, meaning smooth 60fps with minimal CPU usage.</p>
          </div>
        </div>

        <div style="margin-top: var(--space-xl); padding-top: var(--space-lg); border-top: 2px solid var(--card-border);">
          <div style="display: flex; flex-wrap: wrap; gap: var(--space-md);">
            <div style="display: flex; align-items: center; gap: var(--space-sm);">
              <svg width="18" height="18" style="color: var(--success);" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M20 6L9 17l-5-5"/>
              </svg>
              <span style="font-size: 0.9rem; color: var(--text-muted);">No JavaScript runtime</span>
            </div>
            <div style="display: flex; align-items: center; gap: var(--space-sm);">
              <svg width="18" height="18" style="color: var(--success);" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M20 6L9 17l-5-5"/>
              </svg>
              <span style="font-size: 0.9rem; color: var(--text-muted);">No external dependencies</span>
            </div>
            <div style="display: flex; align-items: center; gap: var(--space-sm);">
              <svg width="18" height="18" style="color: var(--success);" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M20 6L9 17l-5-5"/>
              </svg>
              <span style="font-size: 0.9rem; color: var(--text-muted);">No network requests</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       INSTALLATION SECTION
       ═══════════════════════════════════════════════════════════════ -->
  <section class="section" id="install" style="background: var(--bg-alt);">
    <div class="container container-narrow">
      <div class="text-center" style="margin-bottom: var(--space-2xl);">
        <span class="reveal section-label badge-rose">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4M7 10l5 5 5-5M12 15V3"/>
          </svg>
          Installation
        </span>
        <h2 class="reveal d-1">Get Started in 30 Seconds</h2>
        <p class="reveal d-2" style="max-width: 450px; margin: var(--space-sm) auto 0;">
          No package managers, no dependencies, no configuration files. Just download and go.
        </p>
      </div>

      <div class="steps-container">
        <!-- Step 1 -->
        <div class="step-item reveal" data-step="1">
          <div class="flex" style="gap: var(--space-lg);">
            <div class="flex flex-col items-center">
              <div class="step-number">1</div>
              <div class="step-connector"><div class="step-connector-fill"></div></div>
            </div>
            <div class="step-content">
              <h3 class="step-title">Download the Collection</h3>
              <p class="step-desc">Clone the repository or download the ZIP file. Either works.</p>
              <div class="code-block">
                <code><span class="code-keyword">git</span> clone <span class="code-string">https://github.com/flessan/HTMLWallarper.git</span></code>
                <button class="copy-btn" onclick="copyCode(this, 'git clone https://github.com/flessan/HTMLWallarper.git')">Copy</button>
              </div>
            </div>
          </div>
        </div>

        <!-- Step 2 -->
        <div class="step-item reveal d-1" data-step="2">
          <div class="flex" style="gap: var(--space-lg);">
            <div class="flex flex-col items-center">
              <div class="step-number">2</div>
              <div class="step-connector"><div class="step-connector-fill"></div></div>
            </div>
            <div class="step-content">
              <h3 class="step-title">Open Desktop Settings</h3>
              <p class="step-desc">Right-click on your desktop and select "Configure Desktop and Wallpaper" or open System Settings.</p>
              <div class="code-block">
                <code><span class="code-comment"># Or via command line:</span> systemsettings5 kde-desktop</code>
                <button class="copy-btn" onclick="copyCode(this, 'systemsettings5 kde-desktop')">Copy</button>
              </div>
            </div>
          </div>
        </div>

        <!-- Step 3 -->
        <div class="step-item reveal d-2" data-step="3">
          <div class="flex" style="gap: var(--space-lg);">
            <div class="flex flex-col items-center">
              <div class="step-number">3</div>
              <div class="step-connector"><div class="step-connector-fill"></div></div>
            </div>
            <div class="step-content">
              <h3 class="step-title">Select HTML Wallpaper</h3>
              <p class="step-desc">Change the wallpaper type to "HTML Wallpaper" and browse to the wallpaper folder.</p>
              <div class="code-block">
                <code>Wallpaper Type <span class="code-func">→</span> <span class="code-string">"HTML Wallpaper"</span></code>
                <button class="copy-btn" style="pointer-events: none; opacity: 0.5;">Info</button>
              </div>
            </div>
          </div>
        </div>

        <!-- Step 4 -->
        <div class="step-item reveal d-3" data-step="4">
          <div class="flex" style="gap: var(--space-lg);">
            <div class="flex flex-col items-center">
              <div class="step-number">4</div>
            </div>
            <div class="step-content" style="padding-bottom: 0;">
              <h3 class="step-title">Point to Wallpaper Folder</h3>
              <p class="step-desc">Navigate to the specific wallpaper folder you want to use. Each wallpaper has its own directory.</p>
              <div class="code-block">
                <code><span class="code-string">/path/to/HTMLWallarper/wallpapers/</span><span class="code-keyword">your-choice</span>/</code>
                <button class="copy-btn" onclick="copyCode(this, '/path/to/HTMLWallarper/wallpapers/your-choice/')">Copy</button>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="text-center" style="margin-top: var(--space-2xl);">
        <a href="https://github.com/flessan/HTMLWallarper/archive/refs/heads/main.zip" class="reveal btn btn-primary btn-lg confetti-trigger">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4M7 10l5 5 5-5M12 15V3"/>
          </svg>
          Download ZIP (Direct)
        </a>
        <p style="margin-top: var(--space-md); font
